-- Create sample database
-- Note: In PostgreSQL, CREATE DATABASE cannot be executed inside a transaction block.
-- If you are running this script via a tool that wraps everything in a transaction,
-- you may need to configure it to run outside of a transaction.

CREATE DATABASE sample_db;
